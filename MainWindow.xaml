<Window x:Class="GrayScalingApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:GrayScalingApp"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GrayScalingApp.ViewModel"
        Title="Grayscale Demo"
        Width="450" Height="250"
        mc:Ignorable="d">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10" Orientation="Horizontal">
            <TextBlock Margin="0,0,10,0" VerticalAlignment="Center" Text="Toggle Grayscale: " />
            <CheckBox VerticalAlignment="Center" IsChecked="{Binding IsGrayscaleToggled}" />
        </StackPanel>

        <TextBox Grid.Row="1"
                 Margin="10"
                 IsReadOnly="True"
                 Text="{Binding InfoText, Mode=OneWay}" />

        <Border Grid.Row="2"
                Margin="10" Padding="20"
                BorderBrush="Gray" BorderThickness="1">
            <StackPanel>
                <Image Width="64" Height="64" Source="{Binding IconBackground}" HorizontalAlignment="Left" />
            </StackPanel>
        </Border>
    </Grid>
</Window>